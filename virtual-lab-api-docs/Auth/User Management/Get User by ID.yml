info:
  name: Get User by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/auth/users/:vu_id"
  params:
    - name: vu_id
      value: "1234567"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
