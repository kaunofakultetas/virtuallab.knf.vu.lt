info:
  name: Get Templates
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/templates"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
