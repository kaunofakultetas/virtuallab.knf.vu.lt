info:
  name: Validate Template
  type: http
  seq: 6

http:
  method: GET
  url: "{{API_URL}}/templates/:id/validate"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
