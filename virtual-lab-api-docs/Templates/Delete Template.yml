info:
  name: Delete Template
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{API_URL}}/templates/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
