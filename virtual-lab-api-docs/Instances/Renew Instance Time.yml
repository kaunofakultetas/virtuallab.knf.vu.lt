info:
  name: Renew Instance Time
  type: http
  seq: 10

http:
  method: GET
  url: "{{API_URL}}/instances/:instanceId/renew"
  params:
    - name: instanceId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
