info:
  name: Get Instance by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{API_URL}}/instances/:instanceId"
  params:
    - name: instanceId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
