info:
  name: Get Instance Session
  type: http
  seq: 9

http:
  method: GET
  url: "{{API_URL}}/instances/:instanceId/session"
  params:
    - name: instanceId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
