info:
  name: Reboot Instance
  type: http
  seq: 6

http:
  method: GET
  url: "{{API_URL}}/instances/:instanceId/reboot"
  params:
    - name: instanceId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
