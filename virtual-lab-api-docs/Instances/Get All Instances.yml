info:
  name: Get All Instances
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/instances/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
