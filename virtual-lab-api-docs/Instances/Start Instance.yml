info:
  name: Start Instance
  type: http
  seq: 5

http:
  method: GET
  url: "{{API_URL}}/instances/:instanceId/start"
  params:
    - name: instanceId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
