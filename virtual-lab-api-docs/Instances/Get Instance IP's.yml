info:
  name: Get Instance IP's
  type: http
  seq: 8

http:
  method: GET
  url: "{{API_URL}}/instances/:instanceId/ip"
  params:
    - name: instanceId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
