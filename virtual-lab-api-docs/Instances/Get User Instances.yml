info:
  name: Get User Instances
  type: http
  seq: 4

http:
  method: GET
  url: "{{API_URL}}/instances/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
