info:
  name: Create new Instance
  type: http
  seq: 4

http:
  method: POST
  url: "{{API_URL}}/instances/"
  body:
    type: json
    data: |-
      {
        "profile_id": 1,
        "template_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
