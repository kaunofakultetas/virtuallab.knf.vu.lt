info:
  name: Health
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/health"
  auth: inherit

runtime:
  assertions:
    - expression: res.body.status
      operator: eq
      value: ok

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
