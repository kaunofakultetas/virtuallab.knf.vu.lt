info:
  name: Reconcile Network Drift
  type: http
  seq: 6

http:
  method: POST
  url: "{{API_URL}}/network/drift-reconciliations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
