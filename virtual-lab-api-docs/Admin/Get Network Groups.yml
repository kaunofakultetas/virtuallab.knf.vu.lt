info:
  name: Get Network Groups
  type: http
  seq: 4

http:
  method: GET
  url: "{{API_URL}}/network/groups"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5