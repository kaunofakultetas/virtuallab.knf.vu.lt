info:
  name: Get all Guacamole Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/admin/allGuacUsers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
