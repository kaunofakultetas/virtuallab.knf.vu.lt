info:
  name: Get Network Plan
  type: http
  seq: 3

http:
  method: GET
  url: "{{API_URL}}/network/plan"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5